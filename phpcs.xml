<?xml version="1.0"?>
<ruleset name="KathyIsAwesome-CS">
	<description>Kathy is Awesome Coding Standard</description>

	<!-- Only scan PHP files -->
	 <arg name="extensions" value="php" />
    
    <!-- Exclude paths -->
	<exclude-pattern>*/assets/dist/*</exclude-pattern>
	<exclude-pattern>*/bak/*</exclude-pattern>
	<exclude-pattern>*/build/*</exclude-pattern>
	<exclude-pattern>*/node_modules/*</exclude-pattern>
	<exclude-pattern>*/packages/*</exclude-pattern>
	<exclude-pattern>*/src-bakup/*</exclude-pattern>
	<exclude-pattern>*/src-bakup2/*</exclude-pattern>
	<exclude-pattern>*/tests/</exclude-pattern>
	<exclude-pattern>*/vendor/*</exclude-pattern>
	
    <!-- Configs -->
	<config name="minimum_supported_wp_version" value="6.0" />
	<config name="testVersion" value="7.4-" />

    <!-- Rules -->
	<rule ref="WordPress-Extra" />
	<rule ref="WooCommerce-Core" />

		<!-- Rules -->
	<rule ref="WooCommerce-Core">
        <exclude name="WordPress.Files.FileName.NotHyphenatedLowercase" />
		<exclude name="WordPress.Files.FileName.InvalidClassFileName" />
    </rule>

	<rule ref="WordPress.WP.I18n">
		<properties>
			<property name="text_domain" type="array">
				<element value="wc-mnm-interactivity-api"/>
			</property>
		</properties>
	</rule>

	<rule ref="PHPCompatibility">
		<exclude-pattern>tests/</exclude-pattern>
	</rule>
</ruleset>
